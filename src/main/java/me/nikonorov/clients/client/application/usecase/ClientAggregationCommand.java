package me.nikonorov.clients.client.application.usecase;

/**
 * Прикладная команда для сценария агрегации клиента.
 *
 * <p>Входные адаптеры создают эту запись после транспортной валидации и
 * маппинга. Сценарии использования должны зависеть от этого прикладного типа,
 * а не от REST DTO, gRPC-сообщений или других моделей запросов, принадлежащих
 * адаптерам.</p>
 *
 * @param requestId корреляционный идентификатор запроса, переданный вызывающей стороной
 * @param clientId бизнес-идентификатор агрегируемого клиента
 */
public record ClientAggregationCommand(String requestId, String clientId) {
}
